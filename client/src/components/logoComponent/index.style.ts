// import mui module
import { styled } from '@mui/material';

export const LogoStyle = styled('div')(({ theme }: any) => {
    return {
        backgroundColor: theme.colors.darkBlue,
        height: '100%',
        paddingTop: '45%',
        display: "flex",
        justifyContent: "center",

        '.logo-container': {

        }
    }
});